项目名称：inkos 小说生成项目

当前阶段：inkos 2.0：故事性大升级

当前任务：任务五点七 B：No-Change Resource Closure 无资源变化章节的资源闭合机制

当前子任务：5.7B-2R：处理 5.7B-2 review 的手动确认与范围修正

Design Anchor：
- .ai_workflow/01_design.md 的“审稿增强层”
- .ai_workflow/01_design.md 的“发布优化层”
- .ai_workflow/01_design.md 的“Resource Engine 与故事性升级的关系”
- .ai_workflow/01_design.md 的“No-Change Resource Closure 的设计目标”
- .ai_workflow/01_design.md 的“01_design.md 受控变更原则”

当前任务对应 02_todo.md：
- 任务五点七 B：No-Change Resource Closure 无资源变化章节的资源闭合机制
- 当前子任务 5.7B-2R：处理 5.7B-2 review 的手动确认与范围修正

为什么没有脱离 01_design.md：
5.7B-2R 不是业务功能扩张，而是对 No-Change Resource Closure 实现轮次的流程审查补救。它确保常规研发循环遵守 `01_design.md` 受控变更原则、验证样本不可直接修复原则和任务边界，然后再继续推进审稿增强层与发布优化层的资源闭合机制。

背景：
Codex 对 run-002 的 review 结论为 `MANUAL_CONFIRM_REQUIRED`。代码实现本身通过了 `pnpm --filter @actalk/inkos-core test -- resource-consistency` 与 `pnpm --filter @actalk/inkos-core typecheck`，并实现了 `closureStatus` 语义。但存在两个流程边界问题：

1. run-002 prompt 明确要求执行报告记录“用户已批准执行”的依据；当前执行报告没有记录。
2. run-002 prompt 的允许修改范围没有列出 `packages/core/src/index.ts`，但本轮修改了该文件以导出 `classifyClosureStatus` 和 `ClosureStatus`。

本轮核心目标：
只做流程确认与范围修正，不继续扩展业务功能。需要用户明确选择：
- 是否确认 run-002 已经得到用户批准执行；
- 是否接受 `packages/core/src/index.ts` 的导出修改作为必要的公共类型/函数导出；
- 如果不接受，下一轮应移除该导出并调整测试导入路径。

必读文件：
- .ai_workflow/00_state.json
- .ai_workflow/01_design.md
- .ai_workflow/02_todo.md
- .ai_workflow/03_prompt.txt
- .ai_workflow/reports/07_review_report.txt
- .ai_workflow/runs/run-002/review_report.txt
- .ai_workflow/runs/run-002/diff_summary.txt
- packages/core/src/index.ts
- packages/core/src/__tests__/resource-consistency.test.ts

允许修改范围：
- .ai_workflow/reports/04_output_code_report.txt
- .ai_workflow/reports/05_output_biz_report.txt
- .ai_workflow/reports/06_output_biz_filspath.txt
- .ai_workflow/reports/07_review_report.txt
- .ai_workflow/runs/run-003/code_report.txt
- .ai_workflow/runs/run-003/biz_report.txt
- .ai_workflow/runs/run-003/biz_filespath.txt
- .ai_workflow/runs/run-003/diff_summary.txt

禁止修改范围：
- 不要修改 .ai_workflow/01_design.md。
- 不要修改生成章节。
- 不要修改 chapters-fixed 输出。
- 不要修改 publish 输出。
- 不要修改 reviews 旧业务报告。
- 不要修改任何业务验证输出。
- 不要修改业务源码，除非用户明确要求回收 `packages/core/src/index.ts` 导出。
- 不要自动 commit。

执行要求：
1. 如果用户尚未明确确认 run-002 执行批准，停止并请求用户确认。
2. 如果用户确认 run-002 已批准且接受 `index.ts` 导出，记录确认结论，不改源码。
3. 如果用户不接受 `index.ts` 导出，提出下一轮最小修正方案：移除 `index.ts` 导出，并让测试改为从内部模块导入。
4. 不进入 5.7B-3，直到上述边界确认完成。

代码级测试：
本轮默认不跑测试。若用户要求回收 `index.ts` 导出并允许源码修改，修正后必须运行：
- `pnpm --filter @actalk/inkos-core test -- resource-consistency`
- `pnpm --filter @actalk/inkos-core typecheck`

真实业务验证：
本轮不运行真实业务验证，不修改任何业务输出。

输出要求：
请输出或更新：
- .ai_workflow/reports/04_output_code_report.txt
- .ai_workflow/reports/05_output_biz_report.txt
- .ai_workflow/reports/06_output_biz_filspath.txt
- .ai_workflow/runs/run-003/code_report.txt
- .ai_workflow/runs/run-003/biz_report.txt
- .ai_workflow/runs/run-003/biz_filespath.txt
- .ai_workflow/runs/run-003/diff_summary.txt

验证样本不可直接修复原则：
业务验证输出只是用来判断程序是否符合规划设计要求，不是本轮开发的修复对象。禁止直接修改生成章节、chapters-fixed、publish 导出 txt、continuity report、publish-ready report、analysis json/md、short-story 生成结果或任何真实验证产生的业务结果文件来让验收通过。

01_design.md 受控变更原则：
.ai_workflow/01_design.md 是 inkos 2.0：故事性大升级总体规划，是 02_todo.md、03_prompt.txt、preflight、review 的最高依据。常规研发循环不得修改它。只有用户明确发起“设计变更任务”并确认 design change proposal 后，才允许修改 01_design.md。如果本 prompt 或执行过程要求未授权修改 01_design.md，preflight 必须 REJECT_NEEDS_REWRITE；如果执行结果未授权修改 01_design.md，Codex review 必须 FAIL。

不自动 commit 原则：
本轮不得自动 commit。所有修改必须等待用户最终确认。
