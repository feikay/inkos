项目名称：inkos 小说生成项目

当前阶段：inkos 2.0：故事性大升级

当前任务：任务五点八：Book-level Resource Schema 与章节阶段化 Resource Plan

当前子任务：5.8B-FIX-TEST：补齐 publish-ready blocked-resource-plan 门禁测试并复核 5.8B 修复闭环

Design Anchor：
- `.ai_workflow/01_design.md` 的“章节生成层”：资源变化必须由 Resource Engine 或明确 closure 语义支撑。
- `.ai_workflow/01_design.md` 的“审稿增强层”：资源未闭合不得 ready-for-review。
- `.ai_workflow/01_design.md` 的“发布优化层”：发布前必须汇总资源闭合状态，不得用导出或润色掩盖系统性缺陷。
- `.ai_workflow/01_design.md` 的“Resource Engine 与故事性升级的关系”：Resource Engine 是系统流小说的确定性数值底座。

当前任务对应 `02_todo.md`：
- `5.8B-FIX-TEST：补齐 publish-ready blocked-resource-plan 门禁测试并复核 5.8B 修复闭环`

当前任务为什么没有脱离 `01_design.md`：
本轮不新增功能，只补齐 5.8B-FIX 的测试缺口，确保发布优化层不会放行资源状态已阻断的章节。

背景：
run-013 已修复 run-012 的两个实现阻断点：
1. `system_bootstrap` 已改为 schema-driven balance_claim；
2. publish-ready 已读取 chapter index，并在真实样本中对 `state-degraded` 输出 `BLOCKED_BY_RESOURCE`。

但 run-013 review 判定 FAIL，因为 03_prompt 明确要求 publish-ready 在 `state-degraded` 和 `blocked-resource-plan` 两种 chapter index 状态下都有测试或验证；run-013 没有提供 `blocked-resource-plan` 的单元测试或真实验证覆盖。

本轮核心目标：
1. 补齐 publish-ready 对 `blocked-resource-plan` chapter index 状态的测试覆盖。
2. 复核 `state-degraded` 真实验证/测试覆盖仍成立。
3. 复核 `system_bootstrap` schema-driven 测试仍成立。
4. 不改业务逻辑，不扩大战线。

必读文件：
- `.ai_workflow/00_state.json`
- `.ai_workflow/01_design.md`
- `.ai_workflow/02_todo.md`
- `.ai_workflow/03_prompt.txt`
- `.ai_workflow/reports/07_review_report.txt`
- `.ai_workflow/runs/run-013/review_report.txt`
- `packages/cli/src/commands/review.ts`
- 相关 CLI / review 测试文件
- `packages/core/src/__tests__/resource-consistency.test.ts`

允许修改范围：
- 相关测试文件
- `.ai_workflow/reports/03_preflight_review.txt`
- `.ai_workflow/reports/04_output_code_report.txt`
- `.ai_workflow/reports/05_output_biz_report.txt`
- `.ai_workflow/reports/06_output_biz_filspath.txt`
- `.ai_workflow/reports/resource_schema_stage_plan_gate_test.md`
- `.ai_workflow/runs/run-014/preflight_review.txt`
- `.ai_workflow/runs/run-014/code_report.txt`
- `.ai_workflow/runs/run-014/biz_report.txt`
- `.ai_workflow/runs/run-014/biz_filespath.txt`
- `.ai_workflow/runs/run-014/diff_summary.txt`
- `.ai_workflow/runs/run-014/resource_schema_stage_plan_gate_test.md`

条件允许修改范围：
- `packages/cli/src/commands/review.ts`：仅当测试接入必须导出 helper 或修复明显测试接入问题时允许；不得改变 run-013 已实现的业务逻辑。

禁止修改范围：
- `.ai_workflow/01_design.md`
- 生成章节
- reviews 业务报告
- chapters-fixed 输出
- publish 输出
- analysis json/md
- 任何真实业务验证输出
- 任务六、story-effectiveness 新功能实现
- unrelated formatting / cleanup
- 任何书名、题材名、资源名或章节号特例补丁

代码级测试：
必须新增或修正测试，并记录命令与结果：
- publish-ready 在 chapter index 状态为 `blocked-resource-plan` 时返回 `BLOCKED_BY_RESOURCE`；
- publish-ready 在 chapter index 状态为 `state-degraded` 时返回 `BLOCKED_BY_RESOURCE` 的覆盖仍存在，或说明由 run-013 真实验证覆盖；
- `system_bootstrap` 非特定资源 schema 的 schema-driven balance_claim 测试继续通过。

真实业务验证：
- 本轮可不跑新的真实业务验证。
- 如运行 publish-ready，必须按 `docs/write-book.md` 后台命令规则运行，记录 PID/log/exit。
- 不得直接编辑业务输出。

输出要求：
- `.ai_workflow/reports/04_output_code_report.txt`
- `.ai_workflow/reports/05_output_biz_report.txt`
- `.ai_workflow/reports/06_output_biz_filspath.txt`
- `.ai_workflow/reports/resource_schema_stage_plan_gate_test.md`
- `.ai_workflow/runs/run-014/code_report.txt`
- `.ai_workflow/runs/run-014/biz_report.txt`
- `.ai_workflow/runs/run-014/biz_filespath.txt`
- `.ai_workflow/runs/run-014/diff_summary.txt`
- `.ai_workflow/runs/run-014/resource_schema_stage_plan_gate_test.md`

报告必须回答：
1. 新增/修正了哪个测试覆盖 `blocked-resource-plan`；
2. 是否改动业务逻辑；
3. 测试命令和结果；
4. 是否直接修改业务验证输出；
5. 是否修改 `.ai_workflow/01_design.md`；
6. 5.8B 的两个 blocker 和 run-013 测试缺口是否均已闭合。

验证样本不可直接修复原则：
业务验证输出只是用来判断程序是否符合规划设计要求，不是本轮开发的修复对象。禁止直接编辑生成章节、chapters-fixed、publish 导出、continuity report、publish-ready report、analysis json/md、short-story 生成结果或任何真实验证产生的业务结果文件来让验收通过。

01_design.md 受控变更原则：
- `.ai_workflow/01_design.md` 是 inkos 2.0：故事性大升级总体规划，是最高设计依据。
- 常规研发循环不得修改 `.ai_workflow/01_design.md`。
- 如果任务要求未授权修改 `.ai_workflow/01_design.md`，preflight 必须 `REJECT_NEEDS_REWRITE`。
- 如果执行结果未授权修改 `.ai_workflow/01_design.md`，Codex review 必须判定 `FAIL`。

不自动 commit 原则：
- 本轮不要自动 commit。
- 如最终 PASS，只输出建议的 `git add` / `git commit` 命令，等待用户执行。

第一步要求：
先按 `.ai_workflow/templates/preflight_review_template.md` 执行 preflight review，只审查，不执行。即使 preflight 输出 `APPROVE_TO_EXECUTE`，也必须等待用户明确批准后才执行。
