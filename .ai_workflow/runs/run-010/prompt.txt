项目名称：inkos 小说生成项目

当前阶段：inkos 2.0：故事性大升级

当前任务：任务五总体验收：Resource Engine / Resource Plan / No-Change Closure 资源一致性阶段关闭

当前子任务：任务五收束补验：再次执行真实 no-change 验证，目标是观察 `closureStatus: "no_change_closed"`

Design Anchor：
- `.ai_workflow/01_design.md` 的“章节生成层”：资源变化必须由 Resource Engine 或明确 closure 语义支撑，LLM 不得临时发明收益、消耗或余额。
- `.ai_workflow/01_design.md` 的“审稿增强层”：资源未闭合不得 ready-for-review；no-change 章节必须区分“无变化已闭合”和“检查缺失”。
- `.ai_workflow/01_design.md` 的“发布优化层”：发布前不能携带数值污染，不能用导出或润色掩盖系统性缺陷。
- `.ai_workflow/01_design.md` 的“Resource Engine 与故事性升级的关系”：Resource Engine 是系统流小说的确定性数值底座，防止数值污染、资源闭合失败、LLM 乱编收益/消耗。
- `.ai_workflow/01_design.md` 的“No-Change Resource Closure 的设计目标”：区分没有资源变化与资源检查缺失，对无资源变化章节输出明确 closure 结论，不误触发 BLOCKED / WARN。

当前任务对应 `02_todo.md`：
- `任务五收束补验：真实 no-change 验证`

当前任务为什么没有脱离 `01_design.md`：
本轮只补验任务五资源一致性阶段的 no-change closure 真实链路，不开启任务六，不实现 story-effectiveness / publish-ready 新功能。它直接服务于 No-Change Resource Closure 的设计目标。

背景：
run-009 已实际后台运行 `write next` 并生成 post-5.7B 新 resource consistency report，确认 `closureStatus` 字段已进入真实报告输出；但新增章节出现民望值变化和技能解锁，report 为 `closureStatus: "resource_failed"`，不是 no-change 样本。用户要求再做一次真实 no-change 验证。

重要前提：
- `packages/core/logs/` 是 LLM 调用过程日志输出，用户已将该路径加入 `.gitignore`。
- 本轮不要把 `packages/core/logs/` 的日志追加当作任务失败点。
- 但如果有 tracked git diff，仍需在报告中如实记录；不要自行 revert 用户的 `.gitignore` 修改。

本轮核心目标：
1. 实际运行一次真实业务流程，产生新的 post-5.7B 输出。
2. 尽量生成一个没有资源收益、消耗、兑换、技能变化、余额声明的章节。
3. 验证新 resource consistency report 是否出现 `closureStatus: "no_change_closed"`。
4. 若再次生成资源事件，不得手工修改章节或 report，只能如实记录“本次样本仍非 no-change”。
5. 本轮不修改源码，不改 `.ai_workflow/01_design.md`，不自动 commit。

必读文件：
- `.ai_workflow/00_state.json`
- `.ai_workflow/01_design.md`
- `.ai_workflow/02_todo.md`
- `.ai_workflow/03_prompt.txt`
- `.ai_workflow/reports/07_review_report.txt`
- `.ai_workflow/runs/run-009/review_report.txt`
- `.ai_workflow/runs/run-009/task5_resource_stage_closure_report.md`
- `my-novel/books/我使用系统当上美国总统3/chapters/index.json`
- `my-novel/books/我使用系统当上美国总统3/reviews/resource-consistency/0004.report.json`

建议搜索关键词：
- `closureStatus`
- `no_change_closed`
- `resource_failed`
- `noChangeInferred`
- `blocked-resource-plan`
- `state-degraded`
- `Resource Plan`
- `resource consistency`

允许修改范围：
- `.ai_workflow/reports/03_preflight_review.txt`
- `.ai_workflow/reports/04_output_code_report.txt`
- `.ai_workflow/reports/05_output_biz_report.txt`
- `.ai_workflow/reports/06_output_biz_filspath.txt`
- `.ai_workflow/reports/task5_no_change_real_validation_report.md`
- `.ai_workflow/runs/run-010/preflight_review.txt`
- `.ai_workflow/runs/run-010/code_report.txt`
- `.ai_workflow/runs/run-010/biz_report.txt`
- `.ai_workflow/runs/run-010/biz_filespath.txt`
- `.ai_workflow/runs/run-010/diff_summary.txt`
- `.ai_workflow/runs/run-010/task5_no_change_real_validation_report.md`
- `.ai_workflow/runs/run-010/logs/**`
- 程序自然生成的新章节、新 runtime 文件、新 resource consistency report 可作为业务验证输出，但禁止手工编辑。

禁止修改范围：
- `.ai_workflow/01_design.md`
- 业务源码
- 手工修改生成章节
- 手工修改 reviews 报告
- chapters-fixed 输出
- publish 输出
- analysis json/md
- 任务六、story-effectiveness、publish-ready 新功能实现
- unrelated formatting / cleanup

代码级测试：
- 本轮重点是真实业务验证。若本地 dist 可能过期，先运行：
  - `pnpm --filter @actalk/inkos-core build`
  - `pnpm --filter @actalk/inkos build`
- 可选检查：
  - `pnpm --filter @actalk/inkos-core typecheck`
  - `git diff --check`
- `packages/core/logs/` 的 LLM 调用日志按已忽略日志处理，不作为失败点。

真实业务验证要求：
- 必须实际运行程序产生 post-5.7B 新输出，不能只读取旧 report。
- 必须后台运行，不要直接在 console 前台运行。
- 必须记录 PID、日志和退出码。
- 先确认 CLI 参数：
  - `node packages/cli/dist/index.js write next --help`
- 首选业务链路：继续使用 `我使用系统当上美国总统3`，执行前读取 `chapters/index.json`，记录执行前最后章节号和预计新章节号。
- 推荐真实执行命令：
  - `node packages/cli/dist/index.js write next 我使用系统当上美国总统3 --count 1 --words 1200 --context "任务五 no-change 补验：本章只写对话、心理活动、环境观察、局势判断和人物关系压力。不要出现系统面板刷新、奖励、到账、入账、兑换、余额、民望值变化、联邦币变化、技能解锁、技能使用、资源结算或任何数字资源变化。不要为了闭合资源写任何余额声明。必须兑现上一章钩子，但兑现方式只能是信息压力或人物态度变化。" --json`
- 推荐后台执行示例：
  - `mkdir -p .ai_workflow/runs/run-010/logs`
  - `nohup sh -c 'node packages/cli/dist/index.js write next 我使用系统当上美国总统3 --count 1 --words 1200 --context "任务五 no-change 补验：本章只写对话、心理活动、环境观察、局势判断和人物关系压力。不要出现系统面板刷新、奖励、到账、入账、兑换、余额、民望值变化、联邦币变化、技能解锁、技能使用、资源结算或任何数字资源变化。不要为了闭合资源写任何余额声明。必须兑现上一章钩子，但兑现方式只能是信息压力或人物态度变化。" --json; echo $? > .ai_workflow/runs/run-010/logs/write-next.exit' > .ai_workflow/runs/run-010/logs/write-next.log 2>&1 & echo $! > .ai_workflow/runs/run-010/logs/write-next.pid`
- 轮询方式：
  - `cat .ai_workflow/runs/run-010/logs/write-next.pid`
  - `ps -p $(cat .ai_workflow/runs/run-010/logs/write-next.pid)`
  - `tail -n 120 .ai_workflow/runs/run-010/logs/write-next.log`
  - 命令结束后读取 `.ai_workflow/runs/run-010/logs/write-next.exit`

验收读取路径：
- `my-novel/books/我使用系统当上美国总统3/chapters/index.json`
- `my-novel/books/我使用系统当上美国总统3/reviews/resource-consistency/<NNNN>.report.json`
- `my-novel/books/我使用系统当上美国总统3/reviews/resource-consistency/<NNNN>.report.md`
- 必要时只读新增章节正文，判断是否出现资源收益、消耗、兑换、技能变化或余额声明；不得编辑正文。

通过标准：
- 新 report 中出现 `closureStatus: "no_change_closed"`。
- `blocking=false`。
- `resourcePlanViolations` 为空。
- `events` 中没有 gain / consume / unlock / balance_jump。
- chapter index 不进入 `blocked-resource-plan` / `state-degraded`。

如果未通过：
- 如果新章出现资源事件：判定为“本次样本仍非 no-change”，不得手工修正文或 report。
- 如果无资源事件但 report 不是 `no_change_closed`：判定为系统缺陷候选，报告具体 evidence。
- 如果命令失败：记录 exit、日志尾部、失败阶段，不要自行修业务输出。

输出要求：
- `.ai_workflow/reports/04_output_code_report.txt`
- `.ai_workflow/reports/05_output_biz_report.txt`
- `.ai_workflow/reports/06_output_biz_filspath.txt`
- `.ai_workflow/reports/task5_no_change_real_validation_report.md`
- `.ai_workflow/runs/run-010/code_report.txt`
- `.ai_workflow/runs/run-010/biz_report.txt`
- `.ai_workflow/runs/run-010/biz_filespath.txt`
- `.ai_workflow/runs/run-010/diff_summary.txt`
- `.ai_workflow/runs/run-010/task5_no_change_real_validation_report.md`

报告必须回答：
1. 是否实际后台运行真实业务命令；
2. PID/log/exit 文件路径和 exit code；
3. 执行前最后章节号、执行后新增章节号；
4. 新 report 的 `closureStatus`、`blocking`、`status`、`events`、`resourcePlanViolations`；
5. 是否观察到 `closureStatus: "no_change_closed"`；
6. 是否直接修改业务验证输出；
7. 是否修改 `.ai_workflow/01_design.md`；
8. 是否应判定任务五可关闭，或仍为 recorded gap。

验证样本不可直接修复原则：
业务验证输出只是用来判断程序是否符合规划设计要求，不是本轮开发的修复对象。禁止直接编辑生成章节、chapters-fixed、publish 导出、continuity report、publish-ready report、analysis json/md、short-story 生成结果或任何真实验证产生的业务结果文件来让验收通过。

01_design.md 受控变更原则：
- `.ai_workflow/01_design.md` 是 inkos 2.0：故事性大升级总体规划，是最高设计依据。
- 常规研发循环不得修改 `.ai_workflow/01_design.md`。
- 如果任务要求未授权修改 `.ai_workflow/01_design.md`，preflight 必须 `REJECT_NEEDS_REWRITE`。
- 如果执行结果未授权修改 `.ai_workflow/01_design.md`，Codex review 必须判定 `FAIL`。

不自动 commit 原则：
- 本轮不要自动 commit。
- 如最终 PASS，只输出建议的 `git add` / `git commit` 命令，等待用户执行。

第一步要求：
先按 `.ai_workflow/templates/preflight_review_template.md` 执行 preflight review，只审查，不执行。即使 preflight 输出 `APPROVE_TO_EXECUTE`，也必须等待用户明确批准后才执行。
