项目名称：inkos 小说生成项目

当前阶段：inkos 2.0：故事性大升级

当前任务：任务五点七 B：No-Change Resource Closure 无资源变化章节的资源闭合机制

当前子任务：5.7B-6：用真实章节样本验证，不直接修章节输出

Design Anchor：
- .ai_workflow/01_design.md 的“发布优化层”
- .ai_workflow/01_design.md 的“审稿增强层”
- .ai_workflow/01_design.md 的“No-Change Resource Closure 的设计目标”
- .ai_workflow/01_design.md 的“验证样本不可直接修复原则”

当前任务对应 02_todo.md：
- 任务五点七 B：No-Change Resource Closure 无资源变化章节的资源闭合机制
- 当前子任务 5.7B-6：用真实章节样本验证，不直接修章节输出

为什么没有脱离 01_design.md：
5.7B-6 是在代码级测试通过后，用真实章节样本验证 no-change closure 在业务链路中可见、可读、不会污染 publish-ready / continuity / review / export。它只把业务验证输出作为系统验收依据，不直接修复章节、导出、旧报告或分析结果。

背景：
5.7B-2 到 5.7B-5 已完成：
- `closureStatus` 语义定义；
- report JSON / MD 输出；
- BLOCKED / WARN 防污染测试；
- no_change_closed / not_checked / normal_closed / resource_failed 测试覆盖矩阵。

本轮核心目标：
用真实或接近真实的章节样本验证 no-change closure 链路，并记录验证命令、样本路径、输出路径和结论。不得通过直接修改业务输出来让验收通过。

必读文件：
- .ai_workflow/00_state.json
- .ai_workflow/01_design.md
- .ai_workflow/02_todo.md
- .ai_workflow/03_prompt.txt
- .ai_workflow/reports/07_review_report.txt
- .ai_workflow/runs/run-006/review_report.txt
- .ai_workflow/reports/06_output_biz_filspath.txt
- packages/core/src/agents/resource-consistency.ts
- packages/core/src/pipeline/runner.ts
- packages/core/src/__tests__/pipeline-runner.test.ts

建议搜索关键词：
- no_resource_change
- closureStatus
- no_change_closed
- reviews/resource-consistency
- report.json
- report.md
- chapter-index
- ready-for-review
- blocked-resource-plan
- state-degraded

允许修改范围：
- .ai_workflow/reports/04_output_code_report.txt
- .ai_workflow/reports/05_output_biz_report.txt
- .ai_workflow/reports/06_output_biz_filspath.txt
- .ai_workflow/runs/run-007/code_report.txt
- .ai_workflow/runs/run-007/biz_report.txt
- .ai_workflow/runs/run-007/biz_filespath.txt
- .ai_workflow/runs/run-007/diff_summary.txt

禁止修改范围：
- 不要修改 .ai_workflow/01_design.md。
- 不要修改 .gitignore。
- 不要修改业务源码。
- 不要修改生成章节。
- 不要修改 chapters-fixed 输出。
- 不要修改 publish 输出。
- 不要修改 reviews 旧业务报告。
- 不要修改任何业务验证输出。
- 不要自动 commit。

验证要求：
1. 优先寻找已有 no_resource_change / no-change 章节样本。
2. 若没有合适样本，只记录真实验证缺口和建议命令，不伪造输出。
3. 如运行命令会生成业务输出，必须先确认输出路径，并确保不直接编辑这些输出。
4. 检查点至少包括：
   - resource-consistency report JSON 是否含 `closureStatus`；
   - no-change 样本是否能得到 `no_change_closed`；
   - chapter index 或状态不应变为 `state-degraded` / `blocked-resource-plan`；
   - 真实异常样本仍应保持 `resource_failed` / BLOCKED / WARN。

代码级测试：
本轮默认不改代码，不新增测试。若发现系统性缺陷，报告问题来源并建议下一轮回到程序逻辑或测试层修复。

真实业务验证：
必须记录：
- 样本来源；
- 执行命令；
- 生成或读取的报告路径；
- 验证结论；
- 是否修改了业务输出，默认应为“否”。

输出要求：
请输出或更新：
- .ai_workflow/reports/04_output_code_report.txt
- .ai_workflow/reports/05_output_biz_report.txt
- .ai_workflow/reports/06_output_biz_filspath.txt
- .ai_workflow/runs/run-007/code_report.txt
- .ai_workflow/runs/run-007/biz_report.txt
- .ai_workflow/runs/run-007/biz_filespath.txt
- .ai_workflow/runs/run-007/diff_summary.txt

报告至少包含：
- 是否执行真实业务验证；
- 验证命令和样本路径；
- 业务输出路径；
- no-change closure 结果；
- 是否存在系统性缺陷；
- 若未执行真实验证，说明原因和下一步建议。

验证样本不可直接修复原则：
业务验证输出只是用来判断程序是否符合规划设计要求，不是本轮开发的修复对象。禁止直接修改生成章节、chapters-fixed、publish 导出 txt、continuity report、publish-ready report、analysis json/md、short-story 生成结果或任何真实验证产生的业务结果文件来让验收通过。

01_design.md 受控变更原则：
.ai_workflow/01_design.md 是 inkos 2.0：故事性大升级总体规划，是 02_todo.md、03_prompt.txt、preflight、review 的最高依据。常规研发循环不得修改它。只有用户明确发起“设计变更任务”并确认 design change proposal 后，才允许修改 01_design.md。如果本 prompt 或执行过程要求未授权修改 01_design.md，preflight 必须 REJECT_NEEDS_REWRITE；如果执行结果未授权修改 01_design.md，Codex review 必须 FAIL。

不自动 commit 原则：
本轮不得自动 commit。所有修改必须等待用户最终确认。
